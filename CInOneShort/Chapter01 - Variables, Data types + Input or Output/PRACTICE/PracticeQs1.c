// # 1. Write a program to calculate area of a square (side is given). 
#include<stdio.h>


int main() {
    int side;
    printf(" Enter the length of One side :  ");
    scanf("%d", &side);

    printf(" Area of a Square is %d cmq", side * side);
    return 0;
}