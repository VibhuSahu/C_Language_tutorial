#include <stdio.h>

int main(int argc, char const *argv[])
{
    // Diclaration of the values.
    int age = 22;
    float pi = 3.14;
    char star = 'v';


    // Print the diclared values.d
    // 1. INTEGERS
    printf("age is %d \n", age);

    // 2. REAL NUMBER
    printf("Value of pi is %f \n", pi);

    // 3. Characters
    printf("star looks like this %c \n", star);
    return 0;
}
